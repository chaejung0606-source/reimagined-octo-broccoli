# 06. 남은 작업

현재 상태와 다음에 할 일. 새 세션은 여기서 이어받는다.

기준일: 2026-09-08 · 기준 커밋: `d5e191a`

---

## 지금까지 된 것

| 단계 | 범위 | 상태 |
|---|---|---|
| 1 | 파일 분류 · L0 서류 완비성 | ✅ |
| 2 | 텍스트·표 추출 · L1 내부 계산 | ✅ |
| 3 | L2 교차 대사 | ✅ |
| 4 | OCR/Vision — 손글씨 근무일지, 스캔 영수증, 통장·신분증 | ⬜ |
| 5 | L3 한도(규정값 입력 후) · 묶음 모드 동승 중복 검출 | ⬜ |
| 6 | 규칙 편집 UI · 파일별 보기 · 자동 업데이트 · 구글시트 연동 | ✅ |
| 7 | 배포 — 실행 배치파일 · 바탕화면 바로가기 · zip 생성기 | ✅ |

규칙 **87건 중 64건**이 `@check` 로 등록되어 자동 판정된다.
검증: `python -m pytest -q` (80건) · `python tools/validate_rules.py`

---

## A. 규정값이 없어 꺼져 있는 것 — 사용자 확인 필요

L3(규정·한도) 규칙은 값을 모르면 판정할 수 없다. 엔진이 `enabled_if` 로 꺼 두고
결과에 **'규정값 미설정'** 으로 남긴다. 아래 값을 받으면 앱의 [검토 기준] 화면에서
바로 넣을 수 있고, 넣는 즉시 해당 규칙이 켜진다. **코드 수정은 필요 없다.**

1. **근로장학금 지침** — 1일/월 근로시간 한도, 유형별 기준단가, 휴일근로 허용 여부
   → `R-SCH-017` `R-SCH-018` `R-SCH-019` `R-SCH-020`
2. **혁신인재지원금 지침** — 240,000원이 정액인지 시간급인지, 소학회 최소 인원
   → `R-INN-004` `R-INN-016`
3. **강원대 여비 규정** — 일비·식비·숙박비 한도, 자가용 km당 단가,
   공용차량 이용 시 정산 범위 → `R-TRV-025`~`R-TRV-028`
4. **동승 중복청구 처리 원칙** → `R-TRV-020`
5. **반려 기준선** — 🔴 1건이면 반려인지, 담당자 재량인지

---

## B. 아직 판정 함수가 없는 규칙 23건

`@check` 로 등록되지 않아 결과에 **'미구현'** 으로 올라간다. 조용히 통과되지 않는다.

### B-1. OCR/Vision 이 있어야 되는 것 (8건) — 4단계

텍스트 레이어에 값이 없다. 손글씨·스캔 이미지를 읽어야 한다.

| 규칙 | 층위 | 내용 |
|---|---|---|
| `R-CMN-010` | L1 | 서명·날인 누락 |
| `R-CMN-012` | L1 | 필수 입력칸 공란 |
| `R-CMN-014` | L1 | 신분증 주민번호 마스킹 여부 |
| `R-CMN-018` | L1 | 스캔 판독 불가 |
| `R-SCH-006` | L1 | 근무일지 합계칸 공란 |
| `R-SCH-007` | L1 | 근무일지 결재란 서명 누락 |
| `R-INN-018` | L1 | 성과표 실적 미기재 |
| `R-INN-019` | L1 | 증빙서류 체크박스와 실제 첨부 불일치 |

### B-2. 텍스트만으로 가능한데 아직 안 한 것 (11건)

추출기에 필드를 하나씩 더 붙이면 되는 것들이다. **여기가 다음에 손댈 곳이다.**

| 규칙 | 층위 | 내용 | 필요한 것 |
|---|---|---|---|
| `R-CMN-013` | L3 | 재학증명서 발급일 30일 경과 | 증명서 발급일 추출 |
| `R-CMN-015` | L1 | 통장 예금주 불일치 | 통장 사본 예금주 추출 |
| `R-CMN-016` | L1 | 통장 계좌번호 불일치 | 통장 사본 계좌번호 추출 |
| `R-CMN-017` | L2 | 작성일이 활동 종료일보다 이름 | 작성일 필드 |
| `R-SCH-014` | L2 | 강좌명·역할 불일치 | 부분일치 비교 로직 |
| `R-TRV-007` | L1 | 지도 캡처 거리와 불일치 | 지도 이미지 내 거리 텍스트 |
| `R-TRV-014` | L2 | 하이패스 경로 불연속 | 영수증 나들목 순서 검사 |
| `R-TRV-015` | L2 | 인적사항 불일치 | 출장자 명단 대사 |
| `R-TRV-023` | L2 | 정산 불가 품목 포함 | 영수증 품목명 분류 |
| `R-TRV-024` | L2 | 동시간대 상이한 장소 | 영수증 시각·지역 충돌 검사 |
| `R-TRV-020` | L2 | 동승자 중복 청구 의심 | **묶음 모드 전체를 봐야 함**(A-4 도 필요) |

`R-TRV-020` 은 지금 구조로 안 된다. 검토가 지급대상자 한 명 단위로 돌기 때문에
(`4e96dd4` 에서 그렇게 바꿨다) 다른 사람의 영수증을 볼 수 없다. 묶음 검토 결과를
한 번 더 훑는 단계가 필요하다.

### B-3. 남은 4건

`R-TRV-025`~`R-TRV-028` — A-3 의 여비 규정값을 받으면 판정 함수와 함께 붙인다.

---

## C. 품질·운영

- **실제 서류로 돌려 본 적이 없다.** 개인정보 때문에 저장소에 넣을 수 없어
  `tests/conftest.py` 가 추출 결과를 직접 만들어 규칙만 시험한다.
  담당자 컴퓨터에서 실제 폴더로 한 번 돌려 보고, 오탐·미탐을 받아 와야 한다.
- **UI 는 자동 검증이 없다.** PySide6 화면은 사람이 띄워 봐야 안다.
  CI 는 문법 검사(`compileall`)까지만 한다.
- **묶음 검토 성능** — 사람 수가 많으면 PDF 를 매번 다시 연다. 아직 느리다는
  보고는 없다. 느려지면 그때 캐시를 넣는다.

---

## 손대기 전에 읽을 것

- [`CLAUDE.md`](../CLAUDE.md) — 브랜치, 규칙 추가법, 지켜야 할 경계
- [`docs/02-review-criteria.md`](02-review-criteria.md) — 기준 체계 전체
- [`docs/03-field-dictionary.md`](03-field-dictionary.md) — 추출 필드와 신뢰도
- [`docs/04-architecture.md`](04-architecture.md) — 파이프라인
